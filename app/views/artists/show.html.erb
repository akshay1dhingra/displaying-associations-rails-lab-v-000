<h1><%= @artist.name %></h1>

<h4><%= pluralize(@artist.songs.count, "song") %></h4>

<ul>
  <% @artist.songs.each do |song| %>
    <li>
      <%= link_to song.title, song_path(song) %>
    </li>
  <% end %>  
</ul>